import Header from "../Header";
import ThreadsListContainer from "./ThreadsListContainer";

function ThreadsListScreen() {
  return [<Header />, <ThreadsListContainer />];
}

export default ThreadsListScreen;
